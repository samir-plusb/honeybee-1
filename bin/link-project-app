#!/bin/bash

BASEDIR=`readlink -f "$( dirname $0 )/.."`
PWD=`pwd`

cd $BASEDIR

LINK_TARGET='../../project/app'
LINK_LOCATION='app/project'

if [ ! -L $LINK_LOCATION ]
then
    ln -s $LINK_TARGET $LINK_LOCATION
fi

cd $PWD
