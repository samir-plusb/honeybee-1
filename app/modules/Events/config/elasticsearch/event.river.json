{
    "type" : "couchdb",
    "couchdb" : {
        "host" : "localhost",
        "port" : 5984,
        "db" : "{{RUNTIME INJECT}}",
        "filter" : null,
        "script" : "if ('EventsWorkflowItem' === ctx.doc.type) { ctx._type = 'event'; ctx._parent = ctx.doc.ticketId; } else if ('WorkflowTicket' === ctx.doc.type) { ctx._type = 'ticket'; } else { ctx.ignore = true; }"
    },
    "index" : {
        "index" : "{{RUNTIME INJECT}}",
        "bulk_size" : "100",
        "bulk_timeout" : "10ms"
    }
}
