{
    "type" : "couchdb",
    "couchdb" : {
        "host" : "localhost",
        "port" : 5984,
        "db" : "{{RUNTIME INJECT}}",
        "filter" : null,
        "script" : "if ('ShofiVerticalsWorkflowItem' === ctx.doc.type) { ctx._type = 'shofi-vertical'; ctx._parent = ctx.doc.ticketId; } else { ctx.ignore = true; }"
    },
    "index" : {
        "index" : "{{RUNTIME INJECT}}",
        "bulk_size" : "100",
        "bulk_timeout" : "10ms"
    }
}
