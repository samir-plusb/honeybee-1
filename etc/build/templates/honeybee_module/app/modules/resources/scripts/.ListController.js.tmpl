midas.%%MODULE_PREFIX%%.%%MODULE_NAME%%ListController = midas.list.ListController.extend({

    log_prefix: "%%MODULE_NAME%%ListController",

    init: function(options)
    {
        this.parent(options);
    }
});
