honeybee.%%MODULE_PREFIX%%.%%MODULE_NAME%%ListController = honeybee.list.ListController.extend({

    log_prefix: "%%MODULE_NAME%%ListController",

    init: function(options)
    {
        this.parent(options);
    }
});
