honeybee.%%MODULE_PREFIX%%.%%MODULE_NAME%%TreeController = honeybee.tree.TreeController.extend({

    log_prefix: "%%MODULE_NAME%%TreeController",

    init: function(options)
    {
        this.parent(options);
    }
});
