<?php

class %%MODULE_NAME%%DocumentValidator extends AgaviValidator
{
    protected function validate()
    {
        /**
         * @todo implement against dat0r validation.
         */
        $data = $this->getData($this->getArgument());

        if (is_array($data))
        {
            $this->export($data, $this->getArgument());
            return TRUE;
        }
        return FALSE;
    }
}
