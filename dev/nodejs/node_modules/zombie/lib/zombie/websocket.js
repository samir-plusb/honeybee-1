var WebSocket;
WebSocket = require('websocket-client').WebSocket;
exports.use = function() {
  var extend;
  extend = function(window) {
    return window.WebSocket = function(url, proto, opts) {
      return new WebSocket(url, proto, opts);
    };
  };
  return {
    extend: extend
  };
};